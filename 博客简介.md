# SCuNF 博客简介

## 基本信息

| 项目 | 说明 |
|------|------|
| 博客名称 | SCuNF |
| 站点地址 | https://scunf.github.io/ |
| 语言 | 简体中文 (zh-CN) |
| 作者 | SCuNF |

---

## 技术栈

### 核心框架：Valaxy

本博客基于 **Valaxy**（v0.27.0）构建，Valaxy 是一个 **基于 Vue 3 的静态博客框架**（SSG），支持以 Markdown + Vue 组件的方式编写内容。

| 技术 | 版本/说明 |
|------|-----------|
| **框架** | Valaxy v0.27.0 |
| **主题** | valaxy-theme-yun（云）v0.27.0 |
| **运行时** | Vue 3 + TypeScript |
| **构建工具** | Vite + vite-plugin-vue-layouts |
| **CSS** | UnoCSS + SCSS |
| **包管理器** | pnpm（v9） |
| **部署** | GitHub Pages（SSG 静态生成） |

### 架构特点

- **Markdown + Vue**：所有页面和文章均以 `.md` 格式编写，支持 YAML frontmatter
- **SSG 模式**：使用 `valaxy build --ssg` 预渲染为纯静态 HTML，首屏加载快
- **组件自动注册**：`components/` 目录下的 Vue 组件自动全局可用
- **自定义布局**：`layouts/` 目录支持自定义页面布局（如 `project`、`projects`）
- **国际化**：支持中英文双语言切换（`locales/`）
- **PWA 支持**：内置 Service Worker，支持离线访问

---

## 目录结构

```
valaxy-blog
├── pages/                  # 所有页面（Markdown）
│   ├── posts/              # 博客文章
│   ├── project/            # 项目展示页
│   ├── about/              # 关于页面
│   ├── archives/           # 归档
│   ├── categories/         # 分类
│   ├── tags/               # 标签
│   ├── links/              # 友情链接
│   └── girls/              # "老婆列表"页面
├── components/             # 自定义 Vue 组件（自动注册）
│   ├── Counter.vue
│   ├── YunFooter.vue
│   ├── YunGirlItem.vue
│   └── ...
├── layouts/                # 自定义布局
│   ├── project.vue         # 单项目展示布局
│   └── projects.vue        # 项目列表布局
├── locales/                # 国际化语言文件
│   ├── zh-CN.yml
│   └── en.yml
├── styles/                 # 全局样式覆盖
├── public/                 # 静态资源
├── site.config.ts          # 站点元信息配置
├── valaxy.config.ts        # Valaxy 主题与功能配置
├── package.json            # 项目依赖
└── tsconfig.json           # TypeScript 配置
```

---

## 页面功能

| 页面 | 路由 | 说明 |
|------|------|------|
| 文章列表 | `/posts/` | 博客文章主页 |
| 项目列表 | `/project/` | 展示个人项目（robocon、AUPT 等） |
| 友情链接 | `/links/` | 小伙伴们 |
| 老婆列表 | `/girls/` | 喜欢的动漫角色 |
| 关于 | `/about/` | 关于站点与作者 |
| 归档 | `/archives/` | 文章时间归档 |
| 分类 | `/categories/` | 文章分类 |
| 标签 | `/tags/` | 文章标签云 |

### 附加功能

- **RSS 订阅**：`/atom.xml`
- **站内搜索**：内置搜索功能（`search.enable: true`）
- **赞助**：支持赞助展示（`sponsor.enable: true`）
- **ICP 备案**：苏ICP备17038157号

---

## 部署方案

### GitHub Actions 自动部署

Push 到 `master` 分支后，GitHub Actions 自动执行：

1. 安装 pnpm
2. 安装依赖 (`pnpm install`)
3. SSG 构建 (`pnpm run build`)
4. 部署到 gh-pages 分支 (`peaceiris/actions-gh-pages@v4`)

### 其他部署选项

- **Docker**：提供 `Dockerfile` 与 `nginx.conf`
- **Netlify**：`netlify.toml` 配置
- **Vercel**：`vercel.json` 配置

---

## 本地开发

```bash
cd valaxy-blog

# 安装依赖
pnpm install

# 启动开发服务器
pnpm dev
# 访问 http://localhost:4859/

# 构建生产版本（SSG）
pnpm build

# 预览构建结果
pnpm serve
```

---

## 主题特色：Yun（云）

valaxy-theme-yun 是一个简洁清新的博客主题，主要特性包括：

- 首页 Banner 展示
- 多级导航栏
- 自定义页面卡片（pages 配置）
- ICP 备案信息展示
- 丰富的社交链接图标支持（基于 Remix Icon）
- 响应式设计
